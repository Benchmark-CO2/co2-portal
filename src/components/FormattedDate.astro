---
interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time class="text-xl text-accent/50" datetime={date.toISOString()}>
	{
		date.toLocaleDateString('pt-BR', {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>
