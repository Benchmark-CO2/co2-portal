---
import { getCollection } from 'astro:content';
import PrivacyPageContent from '../components/privacidade';
import Layout from '../layouts/Layout.astro';

const privacy = await getCollection("privacy")
const privacyItem = privacy[0]?.data || {};
---

<Layout>
  <PrivacyPageContent client:load privacy={privacyItem} />
</Layout>